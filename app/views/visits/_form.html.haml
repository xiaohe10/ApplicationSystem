= simple_form_for(@visit) do |f|
  = f.error_notification

  .form-inputs
    = f.input :start_date
    = f.input :end_date
    = f.input :visit_type

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
